import 'package:flutter_test/flutter_test.dart';
import 'package:trainer_app/app.dart';

void main() {
  test('TrainerApp is constructible', () {
    expect(const TrainerApp(), isA<TrainerApp>());
  });
}
