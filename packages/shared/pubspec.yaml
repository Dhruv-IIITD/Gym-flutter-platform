name: shared
description: Shared models, services, widgets, and utilities for WTF apps
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  riverpod_annotation: ^4.0.2
  drift: ^2.33.0
  drift_flutter: ^0.3.0
  cloud_firestore:
  firebase_core:
  go_router:
  uuid: ^4.5.1
  intl: ^0.19.0
  shared_preferences: ^2.5.3
  json_annotation: ^4.9.0
  google_fonts:
  shimmer: ^3.0.0
  hmssdk_flutter: ^1.11.1
  permission_handler: ^11.3.1
  http:

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.15
  drift_dev: ^2.25.1
  riverpod_generator: ^4.0.3
  json_serializable: ^6.9.4
  flutter_lints: ^5.0.0
